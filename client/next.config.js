/** @type {import('next').NextConfig} */
const nextConfig = {
	// images: {
	// 	domains: ['links.papareact.com'],
	// },
	// experimental: {
	// 	appDir: true,
	// },
	output: 'export',
}

module.exports = nextConfig;
